name: Check for base package updates

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check-base-updates:
    uses: RXTX4816/cockpit-plugin-base-react/.github/workflows/check-base-updates-plugin.yml@main
    secrets: inherit
