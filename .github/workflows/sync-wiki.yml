name: Sync Wiki

on:
  push:
    branches: [main]
    paths:
      - "docs/wiki/**"

jobs:
  sync:
    uses: RXTX4816/cockpit-plugin-base-react/.github/workflows/sync-wiki-plugin.yml@main
    secrets: inherit
